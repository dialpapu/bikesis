<div class="panel">
  <%if @equipment.empty?  %>
  <p>            </p>
  <div class="row">
    <div class="small-8 large-centered columns">
      <h1 align="center"> Aun no se ha registrado ropa deportiva</h1>
    </div>
  </div>
  <%else%>
  <div class="row">
    <div class="small-12 large-centered columns">
      <h1>Lista de Ropa deportiva - La Ciclería</h1>
    </div>
    <div class="small-12 large-centered columns">

      <table>
        <thead>
          <tr>
            <th>Id</th>
            <th>Referencia</th>
            <th>Producto</th>
            <th>Marca</th>
            <th>Precio</th>
            <th>Descripcion</th>
            <th>Talla</th>
            <th>Genero</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @equipment.each do |equipment| %>
          <tr>
            <td><%= equipment.productId %></td>
            <td><%= equipment.productReference %></td>
            <td><%= equipment.productName %></td>
            <td><%= equipment.productTradeMark %></td>
            <td><%= equipment.productPrice %></td>
            <td><%= equipment.productDescription %></td>
            <td><%= equipment.size %></td>
            <td><%= equipment.gender %></td>
            <td><%= link_to 'Ver', equipment , class: "small button success"%>
            <%= link_to 'Editar', edit_equipment_path(equipment), class: "small button success" %>
            <% if equipment.productStatus == true %>
            <%= link_to 'Deshabilitar', equipment, method: :delete, class: "small button alert", data: { confirm: '¿Estas seguro de deshabilitar esta equipacion?' }%>
            <% else %>
            <%=link_to "Habilitar", equipment, method: :delete, class: "small button success"%>
            <% end %>
          </td>
          </tr>
          <% end %>
        </tbody>
      </table>

      <br>

      <%= link_to 'Nueva equipacion', new_equipment_path , class: "radius button center" %>  
      <%if user_signed_in? && current_user.userType == "Manager"%>
      <%= link_to 'Generar reporte', equipment_index_path(:pdf),class: "radius button center" %>
      <% end %>

      <br>
    </div>
    <br>

  </div>
</div>
<%end%>
</div>