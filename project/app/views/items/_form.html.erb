<%= form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :idVenta %><br>
    <%= f.number_field :idVenta %>
  </div>
  <div class="field">
    <%= f.label :idProducto %><br>
    <%= f.number_field :idProducto %>
  </div>
  <div class="field">
    <%= f.label :cantidadItem %><br>
    <%= f.number_field :cantidadItem %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
